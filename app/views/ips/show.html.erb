<div class="container" style="text-align:center;">
  <h2>Showing IP #<%= @ip.id %></h2>

  <%= render @ip %>

  <div>
  <br>
    <%= link_to "Edit", edit_ip_path(@ip), class: "btn btn-secondary" %> |
    <%= link_to "Back", ips_path, class: "btn btn-primary" %>

  </div>
</div>
